<!DOCTYPE html>
<html>
<head>
    <title>Login Page</title>
    <link rel="stylesheet" type="text/css" href="login_register.css">
</head>
<body>
<div class="container">
    <div class="container">
        <form id="login-form" onsubmit="login(); return false;">
            <h1>Login</h1>
            <label for="username">Username</label>
            <input type="text" id="username" name="username">
            <label for="password">Password</label>
            <input type="password" id="password" name="password">
            <button type="submit">Login</button>
            <p id="message"></p>
        </form>
        <p id="switch">Don't have an account? <a href="register">Register here</a></p>

    </div>
</div>
<script src="script.js"></script>
</body>
</html>
<script>function login() {
        const username = document.getElementById("username").value;
        const password = document.getElementById("password").value;

// Hash the password using SHA-256
        const hashedPassword = sha256(password);

// TODO: Replace this with your own login logic
        if (username === "example" && hashedPassword === "a94a8fe5ccb19ba61c4c0873d391e987982fbbd3") {
// Login successful
            document.getElementById("message").innerHTML = "Login successful!";
            document.getElementById("message").className = "success";
        } else {
// Login failed
            document.getElementById("message").innerHTML = "Username or password incorrect";
            document.getElementById("message").className = "error";
        }
    }
</script>
